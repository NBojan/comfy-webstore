import { Landing, Featured, Services, Contact } from "../components"

const Homepage = () => {
    return (  
        <main>
            <Landing />
            <Featured />
            <Services />
            <Contact />
        </main>
    );
}
 
export default Homepage;